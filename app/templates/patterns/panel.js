define([
    'jquery',
    'mockup-patterns-base'
], function ($, Base) {
    'use strict';

    var panelPattern = Base.extend({
        name: 'panel',
        defaults: {
          // Default values for attributes
        },

        init: function () {
          // The init code for your pattern goes here

        }
    });

    return panelPattern;

});
